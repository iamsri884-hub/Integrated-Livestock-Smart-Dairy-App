name: smart_dairy_app
description: Smart Dairy Management App
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
fl_chart: ^0.68.0
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
